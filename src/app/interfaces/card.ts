export interface FormCard
{
    user_id: string;
    brand_id: string;
    model: string;
    year: number;
    engine_volume: string;
    transmission_type_id: string;
    vin_code: string;
}

export interface CarId
{
    car_id:string
}